import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')

// Ponto de entrada da aplicação Vue.
// Inicializa o app, registra os plugins globais:
// PrimeVue (componentes de UI), Pinia (gerenciamento de estado)
// e Vue Router (navegação entre páginas), e monta o app no DOM.

