// Store Pinia de autenticação.
// Armazena o token JWT e os dados do usuário logado (id, username, is_admin).
// Expõe ações de login (salva o token) e logout (limpa o estado).
// Os dados persistem enquanto a sessão estiver ativa.