// Ponto de entrada do servidor Express.
// Inicializa o app, registra os middlewares globais (como leitura de JSON),
// conecta as rotas e sobe o servidor HTTP na porta definida no .env.
// É o arquivo que o nodemon monitora com "npm run dev".